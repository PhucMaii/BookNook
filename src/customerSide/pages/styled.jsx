import { styled } from '@mui/material/styles';

export const ScrollContainer = styled('div')({
    display: 'flex',
    overflowX: 'auto',
    gap: '20px',
    paddingBottom: '10px',
    width: '100%', // Adjust this width according to your sidebar width
});
